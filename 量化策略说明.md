# 量化策略说明（vnpy 新闻情绪驱动）

> 本目录在 vnpy（VeighNa）开源框架上落地了一套「新闻情绪驱动」量化策略，
> 覆盖 A股 + 美股，跑通历史回测，并给出本地模拟盘（PaperAccount）接入说明。
> **全部内容仅作学习研究，不构成任何投资建议。**

## 一、目录结构

```
chaogu/
├─ config.yaml                         # 标的、阈值、回测参数（统一配置）
├─ strategies/
│  └─ news_sentiment_strategy.py       # vnpy CTA 策略（前视修复+风控+仓位，默认只做多头）
├─ scripts/
│  ├─ download_data.py                 # 拉取行情日线 -> data/*.csv + vnpy SQLite 库
│  ├─ sentiment_score.py               # 新闻情绪打分 -> data/sentiment_*.csv
│  └─ backtest.py                      # 回测：样本内外+参数扫描+买入持有基准 -> results/*.md
├─ data/                               # 行情与情绪 CSV（脚本生成）
├─ results/backtest_report.md          # 回测报告
├─ .vntrader/                          # vnpy 运行目录（数据库 database.db）
└─ GitHub炒股实用项目推荐.md             # 开源项目推荐清单
```

## 二、环境安装

```powershell
# 使用 Python 3.12 创建虚拟环境
C:\Users\Lenovo\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m venv .venv

# 安装依赖（需要联网）
.\.venv\Scripts\python.exe -m pip install vnpy vnpy_ctastrategy vnpy_ctabacktester vnpy_paperaccount vnpy_sqlite vnpy_datamanager akshare pandas pyyaml yfinance
```

> vnpy 默认把运行目录放到用户主目录 `.vntrader`；本目录已预置一个 `.vntrader`，
> 使 vnpy 使用项目内目录，数据库落在 `.vntrader/database.db`。

## 三、运行流程（在项目根目录执行）

### 1. 下载行情

```powershell
.\.venv\Scripts\python.exe scripts\download_data.py
```

- A股：腾讯财经 `stock_zh_a_hist_tx`（前复权）
- 美股：新浪财经 `stock_us_daily`（前复权）
- 结果：`data/bars_{symbol}.csv` + vnpy SQLite 库

### 2. 生成新闻情绪分

```powershell
.\.venv\Scripts\python.exe scripts\sentiment_score.py
```

- 数据源优先级：若存在 `news/daily_sentiment.json`（多源聚合器产物），直接使用其市场+个股情绪；
  否则回退到 akshare 单点抓取（个股新闻 + 东财全球资讯市场情绪）。
- 打分方式：离线中文金融词典（正/负面词计数），无需下载模型。
- 合成情绪：确定性 AR(1) 序列 `data/sentiment_synthetic_{symbol}.csv`，仅用于回测演示策略机制。

### 2.5 回填历史新闻（可选，让回测用上真实历史情绪）

```powershell
.\.venv\Scripts\python.exe scripts\backfill_news.py                 # 回填全部标的并重建历史
.\.venv\Scripts\python.exe scripts\backfill_news.py --market cn     # 只回填 A股
.\.venv\Scripts\python.exe scripts\backfill_news.py --market us     # 只回填美股
.\.venv\Scripts\python.exe scripts\backfill_news.py --no-rebuild    # 只归档，不重建历史
```

- A股：东方财富个股新闻分页回填（免 key，可按代码回填较长时间）。
- 美股：SEC EDGAR 8-K（免 key，全历史）+ Finnhub company-news（免费 key，约 1 年，可选）。
- 回填结果归档到 `news/raw/`，并从归档重建 `news/sentiment_history.json`；之后再跑 `sentiment_score.py` 即消费真实历史情绪。
- Finnhub key：注册 https://finnhub.io/ 免费获取，填入 `.env` 的 `FINNHUB_API_KEY` 或 `config.yaml` 的 `primary.finnhub_api_key`。

### 3. 回测

```powershell
.\.venv\Scripts\python.exe scripts\backtest.py
```

- 使用 vnpy CTA `BacktestingEngine` 逐日回放 K 线；策略内部用「上一交易日」情绪分，T 日成交（已修复前视偏差）。
- 样本内/外：按交易日 60/40 分割，样本外用于验证泛化能力。
- 参数稳健性：对 threshold × stop_loss_pct 做网格扫描，比较样本内/外表现，输出 `results/param_sweep.csv`。
- 输出 `results/backtest_report.md`：总收益、年化、夏普、最大回撤、胜率、成交笔数、买入持有基准等。

## 四、策略逻辑

策略文件：`strategies/news_sentiment_strategy.py`（vnpy `CtaTemplate`）。

```
每个交易日 on_bar（v2，前视修复）:
  情绪分 score = 上一交易日（T-1）新闻情绪，∈ [-1, 1]
  若 pos != 0：先检查风控出场（止损/止盈/移动止损）
  若 pos == 0 且 score >= threshold_long (0.3)      -> 按 risk_percent/lot_size 计算股数买入
  若 pos >  0 且 score <= threshold_flat (-0.3)      -> 卖出全部持仓
  其余情况维持原仓位（只做多头，默认空仓；allow_short=false）

参数（`config.yaml`）：
- `sentiment.threshold_long` / `threshold_flat`：开多/平多阈值
- `sentiment.allow_short`：是否允许做空（A股普通户不可融券，默认 false）
- `sentiment.stop_loss_pct` / `take_profit_pct` / `trailing_stop_pct`：止损/止盈/移动止损（0=关闭）
- `sentiment.risk_percent`：单笔风险占初始资金比例（>0 覆盖 fixed_size）
- `sentiment.max_position_pct`：单标的仓位上限（占初始资金比例）
- `symbols[].fixed_size`：每次开仓股数（risk_percent=0 时生效）
- `symbols[].lot_size`：最小交易单位（A股=100，美股=1）
- `backtest.capital` / `rate` / `slippage` / `pricetick`：资金与交易成本

## 五、模拟盘

### A. 本地模拟盘引擎（已就绪，可立即运行）

```powershell
.\.venv\Scripts\python.exe scripts\paper_trade.py            # 用真实多源情绪分
.\.venv\Scripts\python.exe scripts\paper_trade.py --synthetic # 用合成情绪分演示
.\.venv\Scripts\python.exe scripts\paper_trade.py --reset    # 重置虚拟账户
```

- 撮合规则：T-1 日情绪生成信号，T 日开盘成交（前视修复）；含止损/止盈/移动止损与风险仓位，与策略 v2 一致。
- 持久化 `paper/paper_state.json` + `paper/trades.csv`。
- 重复运行只处理新增交易日，等价于“每日跑一次”的模拟盘。

### B. vnpy PaperAccount（VeighNa Studio，需要实时行情源）


vnpy 的本地仿真撮合由官方 `vnpy_paperaccount` 模块提供。pip 版 `vnpy` 不含完整图形界面，
推荐使用 **VeighNa Studio**（官网下载）运行模拟盘：

1. 安装 VeighNa Studio，并在其 Python 环境中执行：
   ```powershell
   pip install vnpy_paperaccount
   ```
2. 配置实时行情源（任选其一）：
   - 美股：连接 **IB（盈透）TWS 模拟账户**（免费，需注册 IB 账号）。
   - A股：连接券商 CTP 网关，或使用 **TuShare token** 行情。
   - 说明：A股没有完全免费且稳定的实时逐笔行情源，模拟盘撮合依赖实时行情推送。
3. 启动 VeighNa Trader → 勾选 `CtaStrategy` 与 `PaperAccount` 两个功能。
4. 在 CTA 策略界面添加策略，选择导入 `strategies/news_sentiment_strategy.py`，
   按标的填写参数（threshold / threshold_flat / fixed_size / stop_loss_pct / take_profit_pct / trailing_stop_pct / risk_percent / max_position_pct / lot_size / sentiment_path）。
5. 用 PaperAccount 账户进行本地撮合下单（订单不会发往交易所）。

### 无实时行情源时的替代方案

- 每日运行 `scripts/sentiment_score.py` 生成真实情绪分，按阈值人工参考买卖信号。
- 回测脚本本身即“历史数据回放”，已完整演示信号→下单→成交→绩效统计的全流程。

### C. 反转因子模拟盘（--reversal，已通过随机对照与样本外检验）

```powershell
.\.venv\Scripts\python.exe scripts\paper_trade.py --reversal            # 42 只池，独立账户
.\.venv\Scripts\python.exe scripts\paper_trade.py --reversal --reset    # 重置反转账户
```

- 信号：T-1 日收盘跌幅 <= `config.yaml` 的 `reversal.threshold`（默认 -2%）→ T 日开盘全仓买入，
  买入后第 `hold_days`（默认 3）个交易日收盘卖出；A股满足 T+1。
- 依据：反转因子是项目内唯一通过随机对照（p=0.025）与样本外检验（8/9 参数为正）的信号，
  见 `results/event_strategy_report.md` 与 `results/reversal_report.md`。
- 成本敏感：默认成本假设（佣金 0.03% + 滑点 0.01 元/股）下样本外 +6.4%；
  散户级执行（佣金 0.1% + 滑点 0.05）下净收益趋零，实盘前务必评估自身执行成本。
- 状态隔离：`paper/reversal_state.json` + `paper/reversal_trades.csv`，与情绪模式互不影响。
- 注意：组合收益右偏（少数强趋势标的贡献大头），个别账户波动极大，仅作研究参考。

## 六、已知限制

- 免费快讯源只返回最近数日新闻；历史情绪可改用 `scripts/backfill_news.py` 回填
  （A股东财分页 + 美股 SEC 8-K/Finnhub），但 A股分页回填的实际深度需在有网环境实测确认。
- 美股个股新闻接口（yfinance）在本次运行期间被限流，使用东财全球资讯市场情绪作为代理。
- 情绪打分 v1 为离线词典，较粗；可替换为 FinBERT（`yiyanghkust/finbert-tone`）等模型提升精度。

## 六、多源新闻聚合器（GitHub Actions + 企业微信推送）

聚合器位于 `news_aggregator/`，汇聚财联社、东财全球资讯、新浪7×24、同花顺7×24、富途牛牛、
华尔街见闻、金十快讯等源（政策公告为 best-effort 可选源），逐源容错。

本地运行：

```powershell
.\.venv\Scripts\python.exe news_aggregator\run.py            # 抓取 + 生成日报
.\.venv\Scripts\python.exe news_aggregator\run.py --push     # 并推送到企业微信
```

产物：
- `news/raw/{date}.jsonl`、`news/daily_sentiment.json`、`news/report/{date}.md`

GitHub Actions 定时：
1. 建 GitHub 仓库并 push 本目录（当前本地仓库尚无 remote）。
2. 添加 Actions secret：`WECOM_WEBHOOK`（企业微信群机器人 webhook）。
3. 工作流每个工作日 18:30（北京时间）自动运行：抓取→生成→推企业微信→commit `news/` 回仓库。
4. 本地 `git pull` 后运行 `scripts/sentiment_score.py`，策略即消费最新多源情绪。
## 七、验收对照

- ✅ 推荐报告含 ≥15 个真实项目、新闻舆情类 ≥6 个。
- ✅ `.venv` 可启动，vnpy/akshare 可正常 import。
- ✅ AKShare 成功拉取 2 只 A股 + 2 只美股日线入库。
- ✅ 情绪脚本产出每日情绪分 CSV；回测无报错并产出含夏普/回撤/胜率的报告。
- ✅ 模拟盘接入步骤已文档化（见上），无实时行情源时以历史回放替代。
## 八、实时事件监测器（秒级告警，只告警不下单）

`news_aggregator/monitor.py` 是独立于日频聚合器的常驻进程，部署在大陆轻量云服务器上 24/7 运行：

```powershell
.\.venv\Scripts\python.exe news_aggregator\monitor.py               # 常驻轮询（默认 15s）
.\.venv\Scripts\python.exe news_aggregator\monitor.py --once        # 只跑一轮
.\.venv\Scripts\python.exe news_aggregator\monitor.py --dry-run     # 只检测不推送
```

工作流：轮询快讯源 → 按 id 去重（`news/seen.json`）→ 情绪打分 → 事件词匹配（`themes.yaml`）
→ 板块/个股映射（`boards.py`，AKShare 概念/行业板块，运行时对齐）→ 多通道告警（企业微信/Server酱/Telegram/ntfy）。

- 首次运行只建立基线、不告警历史旧闻；之后每出现一条命中主题的新消息即立即推送。
- 板块接口在部分网络环境不可用，失败时自动降级（告警仍正常，仅缺少板块/成分股信息）。
- 云部署：`sudo bash scripts/install_service.sh /opt/chaogu`（systemd 自启+崩溃重启），或 `docker compose up -d --build`。
- 主题库：编辑 `news_aggregator/themes.yaml` 即可增删主题，无需改代码。